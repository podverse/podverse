# 04 — Home Maestro screenshot flows

Implement master steps **8.14–8.15**.

## Detail docs

- 253-e2e-home-media-types-screenshots, 254-e2e-media-type-swipe

## Tasks

1. Add `apps/mobile/e2e/home.yaml` (or `home-media-types.yaml`): login via E2E API profile,
   screenshot Home per media-type selection.
2. Add a selector swipe/scroll flow asserting feed content changes.
3. Follow **mobile-e2e-screenshots**; use Track 5 seed/env; output to standard report slots.
4. Mark **8.14–8.15** / **253–254** `done`.

Do not run tests during agent work (operator runs Maestro at end).
