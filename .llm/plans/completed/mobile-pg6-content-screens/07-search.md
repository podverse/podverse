# 07 — Search screen + filters/sort

Implement master steps **9.8–9.9**.

## Detail docs

- 267-search-screen, 268-search-filters-sort

## Tasks

1. Search screen (Search tab root): debounced query input + results list via Podcast Index search
   `req*`; tap → podcast detail.
2. Filters/sort matching web defaults; re-query on change.
3. Reuse channel/podcast rows + state components; `testID`s `search-input`/`search-results`.
4. Mark **9.8–9.9** / **267–268** `done`.

Follow **mobile-theme-parity** § Screen & visual parity (web `app/search`, `components/PodcastIndex`,
`components/List/SearchResults`). Do not run tests during agent work.
