# 06 — Album, artist, clip detail

Implement master steps **9.5–9.7**.

## Detail docs

- 264-album-detail-screen, 265-artist-detail-screen, 266-clip-detail-screen

## Tasks

1. Album detail: cover header + track list (reuse track row).
2. Artist detail: header + albums/tracks sections (reuse album card + track row).
3. Clip detail: header + play-at-clip-bounds action passing start/end to the playback hook.
4. Mark **9.5–9.7** / **264–266** `done`.

Follow **mobile-theme-parity** § Screen & visual parity (web `app/album`, `app/artist`, `app/clip`,
`components/List/Music`, `components/Clip`). Do not run tests during agent work.
