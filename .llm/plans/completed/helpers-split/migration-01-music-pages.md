# Migration Part 1: Music Pages QueryParams

## Scope

Update QueryParams imports in music-related page components.

## Files to Update (4)

### 1. `apps/web/src/app/albums/page.tsx`

**Current issue**: Imports `QueryParamsMedium` from `@podverse/helpers`
**Action**: No change needed - `QueryParamsMedium` correctly stays in helpers

### 2. `apps/web/src/app/artists/page.tsx`

**Current issue**: Imports `QueryParamsMedium` from `@podverse/helpers`
**Action**: No change needed - `QueryParamsMedium` correctly stays in helpers

### 3. `apps/web/src/app/tracks/page.tsx`

**Current issue**: Imports `QueryParamsMedium` from `@podverse/helpers`
**Action**: No change needed - `QueryParamsMedium` correctly stays in helpers

### 4. `apps/web/src/app/music/livestreams/page.tsx`

**Current issue**: Imports `QueryParamsMedium` from `@podverse/helpers`
**Action**: No change needed - `QueryParamsMedium` correctly stays in helpers

## Result

All music page files are already correct! `QueryParamsMedium` should stay in `@podverse/helpers`.

## Status

✅ No changes needed
