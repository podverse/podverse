# Execution order — mobile-track9d-playlist-authoring

Run COPY-PASTA prompts **1 → 3** in order.

## Phase 1 — sequential

1. **01** — Create + edit playlist screens (9d.1, 9d.2)
2. **02** — Reorder + add-to-playlist more sheet (9d.3, 9d.4)
3. **03** — Stack header back sketch (9d.5) + light Maestro smoke

## Parallelism

None (shared navigation + playlist detail files).

## After each prompt

- Mark `[x]` in `COPY-PASTA.md`
- Flip affected master steps + Appendix C + detail headers to `done`
- Do **not** run tests during agent work
