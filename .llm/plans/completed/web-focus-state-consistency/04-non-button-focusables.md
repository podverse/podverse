# 04 — Sliders

## Deliverables

- `VolumeSlider`: inner track wrapper holds `overflow: hidden`; outer bar gets `:focus-visible` ring
  via UI focus mixin `@use`.
- `MediaPlayerProgress.module.scss`: `:focus-visible` ring on `.customProgressBar`.
