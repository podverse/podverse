# Phase 06 — Tests

- Extend `productMembership.integration.test.ts` (partial PATCH, empty body 400).
- ORM tests for `BillingPriceCatalogService` caps.
- `products-hub.spec.ts`: edit icons + modal + mock PATCH + POST schedule.
