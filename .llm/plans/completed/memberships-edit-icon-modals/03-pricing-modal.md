# Phase 03 — Pricing modals

- `scheduleProductPricing` in `productPricing.ts` → `POST /product/pricing/schedule`.
- Monthly/annual rows: edit icon → modal → refresh `getResolvedProductMembership` + `getActiveProductPricing`.
