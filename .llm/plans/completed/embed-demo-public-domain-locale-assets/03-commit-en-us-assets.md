# Phase 03 — Commit en-US assets

## Tasks

1. Run import script for `en-US` only first.
2. Remove legacy flat `embed-demo/audio/*.mp3` tone files after locale tree exists.
3. Verify file sizes stay within ~25 MB for en-US.
4. Keep `en-US` fixture IDs unchanged (`embSmpEpAud1`, etc.).

## Acceptance

- `episode-primary.mp3` plays 60s of Duck and Cover excerpt
- Alternate OGG/MP4/WebM are time-synced (same in-point)
