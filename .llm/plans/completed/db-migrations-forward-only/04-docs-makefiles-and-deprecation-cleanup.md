# Phase 4 - Podverse docs, makefiles, and legacy reference cleanup

## Scope

Finalize forward-only migration rollout by removing remaining old-process references and documenting the new operating model.

## Key files

- `docs/operations/DB-MIGRATIONS.md` (new)
- `makefiles/local/Makefile.local.infra.mk`
- `makefiles/local/Makefile.local.test.mk`
- `makefiles/alpha/Makefile.alpha.infra.mk`
- `package.json`
- `.github/workflows/ci.yml`
- `infra/docker/ci/INFRA-DOCKER-CI.md` (if migration process is documented there)

## Steps

1. **Author migration runbook**
   - Add a clear doc for:
     - creating a new migration;
     - running migrations locally;
     - baseline onboarding for existing DBs;
     - running K8s ops one-off jobs;
     - expected idempotent behavior and troubleshooting.

2. **Update developer/test workflows**
   - Replace text that says schema is regenerated by combine script.
   - Point test setup to forward-only bootstrap + linear update process.

3. **Purge outdated naming**
   - Remove stale mentions of previous migration terminology.
   - Keep docs focused on forward-only migration commands and workflow.

4. **Finalize cleanup**
   - Ensure obsolete scripts are deleted.
   - Confirm no remaining Makefile/npm/CI/documentation path relies on old commands.

## Verification

- `rg` searches show only forward-only migration terminology in active docs/workflows.
- Docs and Makefile help text align with forward-only workflow.
- New runbook includes both first-boot and existing-db migration paths.
