#####
##### podverse-web (main Next.js container)
#####
# Used for infra/config/local/web.env and alpha web.env.
# RUNTIME_CONFIG_URL is set here; make local_env_setup copies Observability and Extensions
# from infra/config/local/web-sidecar.env (from apps/web/sidecar/.env.example).
# Runtime-config JSON: apps/web/sidecar/.env.example (Integrations + NEXT_PUBLIC_*).

#####
##### Runtime Config Sidecar
#####
RUNTIME_CONFIG_URL="http://localhost:3001"
