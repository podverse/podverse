# Version: 1
# This Service exposes the Management Web frontend internally on port 3100.

apiVersion: v1
kind: Service
metadata:
  name: podverse-management-web
spec:
  type: ClusterIP
  selector:
    app: podverse-management-web
  ports:
    - name: http
      port: 3100
      targetPort: 3100
      protocol: TCP
