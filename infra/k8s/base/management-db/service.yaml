apiVersion: v1
kind: Service
metadata:
  name: podverse-management-db
spec:
  type: ClusterIP
  selector:
    app: podverse-management-db
  ports:
    - protocol: TCP
      port: 5999
      targetPort: 5432
