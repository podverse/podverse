# Version: 1
# This Service exposes the Management API internally on port 3100.

apiVersion: v1
kind: Service
metadata:
  name: podverse-management-api
spec:
  type: ClusterIP
  selector:
    app: podverse-management-api
  ports:
    - name: http
      port: 3100
      targetPort: 3100
      protocol: TCP
