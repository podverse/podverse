NODE_ENV=production
RUNTIME_CONFIG_URL=http://localhost:3001

# Observability — main Next.js container (see docs/operations/observability/TRACING.md).
OTEL_SERVICE_NAME=podverse-web
OTEL_TRACES_EXPORT=none
# Extensions toggle and OTLP export → podverse-extensions-config (alpha/common).
