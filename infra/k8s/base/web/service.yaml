# Version: 1
# This Service exposes the Web frontend internally on port 3002.

apiVersion: v1
kind: Service
metadata:
  name: podverse-web
spec:
  type: ClusterIP
  selector:
    app: podverse-web
  ports:
    - name: http
      port: 3002
      targetPort: 3002
      protocol: TCP
