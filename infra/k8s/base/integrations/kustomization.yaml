# Shared integrations configuration (podverse-integrations-config).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: podverse-integrations-config
    envs:
      - source/integrations.env

generatorOptions:
  disableNameSuffixHash: true
