# Version: 4
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - service.yaml
  - deployment.yaml

configMapGenerator:
  - name: podverse-api-config
    envs:
      - source/api.env

generatorOptions:
  disableNameSuffixHash: true

replacements:
  - source:
      kind: Service
      name: podverse-api
      fieldPath: spec.ports.0.port
    targets:
      - select:
          kind: Service
          name: podverse-api
        fieldPaths:
          - spec.ports.0.targetPort
      - select:
          kind: Deployment
          name: podverse-api
        fieldPaths:
          - spec.template.spec.containers.0.ports.0.containerPort
          - spec.template.spec.containers.0.readinessProbe.httpGet.port
          - spec.template.spec.containers.0.livenessProbe.httpGet.port
      - select:
          kind: ConfigMap
          name: podverse-api-config
        fieldPaths:
          - data.API_PORT # <--- Dynamically updates the API_PORT env var
