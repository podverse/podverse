pipeline {
    agent {
        label 'pv-alpha-srv'
    }

    options {
        disableConcurrentBuilds()
        timestamps()
    }

    stages {
        stage('Stop Alpha Management Web') {
            steps {
                script {
                    sh 'make -f makefiles/alpha/Makefile.alpha.mk alpha_management_web_down'
                }
            }
        }
    }
}
