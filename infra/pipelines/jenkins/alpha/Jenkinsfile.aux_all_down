pipeline {
    // Maps to <assignedNode>pv-alpha-aux</assignedNode>
    agent {
        label 'pv-alpha-aux'
    }

    // Maps to <concurrentBuild>false</concurrentBuild>
    options {
        disableConcurrentBuilds()
        timestamps()
    }

    stages {
        stage('Stop Alpha Services') {
            steps {
                script {
                    // Maps to 'pushd /opt/podverse'
                    // The dir() block sets the working directory for the contained steps
                    dir('/opt/podverse') {
                        // Maps to 'make alpha_all_down'
                        sh 'make -f makefiles/alpha/Makefile.alpha.mk alpha_all_down'
                    }
                }
            }
        }
    }
}