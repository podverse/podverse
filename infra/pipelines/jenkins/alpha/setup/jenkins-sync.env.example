# Jenkins base URL (no trailing slash recommended)
JENKINS_URL="https://jenkins.example.com"

# Path to credentials file containing username:api_token (single line)
JENKINS_CREDENTIALS_FILE="$HOME/.jenkins-api-token"

# Target Jenkins folder path used by import script
JENKINS_FOLDER="pipelines/alpha"
