pipeline {
    agent {
        label 'pv-alpha-aux'
    }

    options {
        disableConcurrentBuilds()
        timestamps()
    }

    stages {
        stage('DB Reset (management db)') {
            steps {
                script {
                    dir('/opt/podverse') {
                        sh 'make -f makefiles/alpha/Makefile.alpha.mk alpha_management_db_reset'
                    }
                }
            }
        }
    }
}
