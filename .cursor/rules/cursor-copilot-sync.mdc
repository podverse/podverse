---
description: Cursor/Copilot sync policy - keep Cursor source files and Copilot mirrors aligned
globs:
  - ".cursor/skills/**"
  - ".cursor/rules/**"
  - ".cursorrules"
  - ".github/skills/**"
  - ".github/instructions/**"
  - ".github/copilot-instructions.md"
  - "scripts/development/llm/cursor-to-copilot-*.mjs"
alwaysApply: true
---

# Cursor/Copilot Sync Policy

Source of truth is Cursor customization content:

- `.cursor/skills/**`
- `.cursor/rules/**`
- `.cursorrules`

VS Code Copilot files under `.github/**` must be kept in sync in the same change.

## Required Workflow

1. Edit Cursor source files first.
2. Run sync tooling:
   - `npm run llm:sync:cursor-copilot`
3. Validate mirrored files:
   - `npm run llm:sync:cursor-copilot:check`
4. Include both source and mirrored updates in the same commit.

## Mapping

- `.cursor/skills/<name>/SKILL.md` -> `.github/skills/<name>/SKILL.md`
- `.cursor/rules/<rule>.mdc` -> `.github/instructions/<rule>.instructions.md`
- `.cursorrules` -> `.github/copilot-instructions.md`

## Hard Guardrails

- If any Cursor source customization file changes, its Copilot mirror must also change in the same commit.
- If a required mirror file does not exist yet, create it in the same change.
- If `npm run llm:sync:cursor-copilot:check` fails, stop and fix sync issues before continuing.
- Do not finalize implementation summaries while sync validation is failing.
