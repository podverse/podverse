---
description: Mobile auth 401 handling must terminate — never refresh into a token-change loop
globs:
  - "apps/mobile/src/auth/**"
  - "apps/mobile/src/**/*Request*"
  - "apps/mobile/src/push/**"
  - "apps/mobile/src/popularityTracking/**"
  - "apps/mobile/src/hooks/useManagedCopy.ts"
alwaysApply: false
---

# Mobile — every 401 path must terminate

A 401 on a mobile authenticated request must end in **valid credentials** or **`clearSession`**.
It must never leave the app authenticated with credentials the server has already refused after a
refresh. That pattern is how an ended session turns into an infinite request storm.

## Why this exists

`accessToken` is in the dependency list of many effects (notifications unread count, popularity
tracking, queue loaders, FCM sync). Each `setTokens` call gives those effects a new token value and
they fire again. If a refresh returns 200 but the next request is still 401, and the client only
rethrows without clearing, every effect retries forever.

## Do

- Route authenticated product requests through `requestWithMobileAuthRefresh`.
- Refresh once before sending when the access token's `exp` is missing, unreadable, or inside the
  60-second skew window. Do not send an expired access token to provoke a 401.
- Run that gate on every bearer request, including one that starts after the access token aged out
  while the app stayed open. The caller's action proceeds with the refreshed token.
- Keep the 401 retry for a token the client treated as still valid (clock skew, or expiry after the
  request left the device).
- Publish the latest pair from `setTokens` and from hydrate, and clear it when the session
  generation advances, so a closure that captured an older access token sends the newer one.
- When a **just-refreshed** access token is still rejected with 401, call
  `clearSession('session_expired')` before rethrowing. Do not refresh a second time in that call.
- When the **refresh** call itself is 401 (or reuse-detected), call `clearSession('session_expired')`.
- Advance the auth session generation inside `clearSession` **before** any await, so an in-flight
  refresh cannot write tokens after sign-out and resurrect the session.
- Keep `clearSession` single-flight: concurrent 401s share one clear.
- Leave web, management-web, and the APIs without a matching client gate. A 401 storm from an
  expired mobile access token is a bearer-client bug. Logout unregister stays on the raw client so
  sign-out does not refresh and write tokens before `clearSession('user_logout')`.

## Don't

- Don't send an expired or unreadable access token as the first attempt.
- Don't handle a 401 by refreshing, calling `setTokens`, retrying, getting 401 again, and only
  rethrowing — that is the loop.
- Don't add a browser access/refresh pair or change API token lifetimes to match this gate.
- Don't key an effect on `accessToken` in a way that **itself** causes a token change on failure.
  Reading the token to build a request is fine; writing a new token from that effect's failure path
  without ending the session is not.
- Don't treat a network timeout or 5xx as session expiry — those leave the session intact so an
  offline device stays signed in (`SessionEndReason` / `forcedLogoutNotice`).

## Related

- [`mobile-account-scoped-data`](/.cursor/rules/mobile-account-scoped-data.mdc) — what `clearSession`
  must wipe
- [`mobile-sync-orchestration`](/.cursor/rules/mobile-sync-orchestration.mdc) — token refresh is
  orthogonal to the sync queue
- Implementation: `apps/mobile/src/auth/authRequestWithRefresh.ts`,
  `apps/mobile/src/auth/AuthProvider.tsx`
