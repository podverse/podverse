appId: com.podverse.app.next
name: Add By RSS Flow
---
# Add-by-RSS library in My Library: plus opens the add form; the list is feeds only. Detail and
# playback stay on the Library stack. Fixture URL matches web e2e seed constants.

- runFlow: shared/launch-and-connect.yaml
- runFlow: shared/login-seeded-user.yaml

- tapOn:
    id: tab-my-library
- assertVisible:
    id: library-hub-screen
- tapOn:
    id: library-nav-add-by-rss
- assertVisible:
    id: rss-root-screen
- takeScreenshot: add-by-rss-library-initial

- tapOn:
    id: rss-add-button
- assertVisible:
    id: rss-add-screen
- assertVisible:
    id: rss-url-input
# Username and password stay behind the Basic Auth toggle (off by default).
- assertNotVisible:
    id: rss-username-input
# Add stays disabled until the URL is a valid http(s) feed URL.
- assertVisible:
    id: rss-add-submit
    enabled: false
- takeScreenshot: add-by-rss-add-screen

- tapOn:
    id: rss-url-input
- inputText: https://e2e-seed-addbyrss.example/podcast.xml
- assertVisible:
    id: rss-add-submit
    enabled: true
- tapOn:
    id: rss-add-submit
- takeScreenshot: add-by-rss-after-submit

# After a successful add, the form pops back to the library list with the feed among the rows.
- extendedWaitUntil:
    visible:
      id: rss-root-screen
    timeout: ${TIMEOUT_SLOWER}
- assertVisible: E2E Add-by-RSS Channel
- takeScreenshot: add-by-rss-feed-listed

- runFlow:
    when:
      platform: Android
    commands:
      - hideKeyboard
- tapOn:
    text: E2E Add-by-RSS Channel
- assertVisible:
    id: add-by-rss-home-detail-screen
- assertVisible: E2E Add-by-RSS Channel
- assertVisible:
    id: add-by-rss-home-play-first
- takeScreenshot: add-by-rss-library-detail

# Real media: enclosure localhost:2111 → 127.0.0.1 (iOS) / 10.0.2.2 (Android) in E2E.
- tapOn:
    id: add-by-rss-home-play-first
- extendedWaitUntil:
    visible:
      id: add-by-rss-home-playback-active
    timeout: ${TIMEOUT_SLOWEST}
- takeScreenshot: add-by-rss-playback-active

- tapOn:
    id: add-by-rss-home-remove
- extendedWaitUntil:
    visible:
      id: rss-root-screen
    timeout: ${TIMEOUT_SLOW}
- assertNotVisible: E2E Add-by-RSS Channel
