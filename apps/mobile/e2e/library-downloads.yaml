appId: com.podverse.app.next
name: Library Downloads Offline Play
---
# Play a seeded progressive episode while it is still streaming, then download that same file.
# When the download completes, playback switches to the local file and keeps playing.
# Confirm the episode appears in My Library -> Downloads under the Completed section.
# Storage usage lives under More → Settings → Downloads.
#
# Fixture: a seeded podcast episode with a progressive mp3 enclosure on the test-assets server
# (:2111), non-live and non-HLS (livestream / m3u8 items never render the Download control). The
# download runner applies the same loopback-host rewrite as playback (resolveE2eMediaUrl) so the
# transfer works on the iOS simulator (127.0.0.1) and Android emulator (10.0.2.2).
#
# Limitation: Maestro cannot reliably toggle airplane mode, so this proves the
# stream -> download complete -> local handoff (playback-source-e2e flips remote to local)
# rather than literally cutting the network. True offline playback is verified manually.
# Needs flow_needs_e2e_api + flow_needs_test_assets (registered in scripts/mobile/e2e-test.sh).

- runFlow: shared/launch-and-connect.yaml
- runFlow: shared/login-seeded-user.yaml

# Navigate to a seeded podcast the account does not follow, so the completed file
# lands on Home under Downloaded only (not on a subscribed row).
- tapOn:
    id: tab-search
- assertVisible:
    id: search-screen
- tapOn:
    id: search-input
- inputText: "Cosmos Classroom"
- assertVisible:
    id: search-result-row-0
- tapOn:
    id: search-result-row-0
- assertVisible:
    id: podcast-detail-screen
- assertVisible:
    id: podcast-episode-row-0
- tapOn:
    id: podcast-episode-row-0
- assertVisible:
    id: episode-detail-screen
- takeScreenshot: library-downloads-episode-detail

# Play first, while the enclosure is still remote.
- tapOn:
    id: "home-row-play-.*"
- extendedWaitUntil:
    visible:
      id: playback-active-e2e
      text: playing
    timeout: ${TIMEOUT_SLOW}
- extendedWaitUntil:
    visible:
      id: playback-source-e2e
      text: remote
    timeout: ${TIMEOUT_SLOW}
- assertVisible:
    id: mini-player

# Download the episode that is streaming. Completion swaps the engine onto the local file
# without stopping playback.
- assertVisible:
    id: episode-detail-download
- tapOn:
    id: episode-detail-download
- extendedWaitUntil:
    visible:
      id: episode-detail-download-complete
    timeout: ${TIMEOUT_SLOWEST}
- extendedWaitUntil:
    visible:
      id: playback-source-e2e
      text: local
    timeout: ${TIMEOUT_SLOW}
- assertVisible:
    id: playback-active-e2e
    text: playing
- takeScreenshot: library-downloads-episode-downloaded
- takeScreenshot: library-downloads-offline-now-playing

# The completed download appears in My Library -> Downloads. Finished jobs are not in-progress,
# so the Downloads row and My Library tab badges stay hidden. Storage chrome is not on this screen.
- tapOn:
    id: tab-my-library
- assertVisible:
    id: library-hub-screen
- assertNotVisible:
    id: library-nav-downloads-badge
- tapOn:
    id: library-nav-downloads
- assertVisible:
    id: library-downloads-screen
- assertVisible:
    id: "download-row-.*"
- assertVisible:
    id: "download-row-channel-.*"
- assertNotVisible:
    id: library-downloads-clear-finished
- assertNotVisible:
    id: library-downloads-usage
- takeScreenshot: library-downloads-list

# Cosmos Classroom is not a follow, so the completed file is only on Home under Downloaded only.
# Swipe-left Delete all removes that show's files and the footer row.
- tapOn:
    id: tab-home
- assertVisible:
    id: home-screen
- assertVisible:
    id: home-unsubscribed-downloads
- swipe:
    from:
      id: "home-unsubscribed-download-row-.*-swipe"
    direction: LEFT
- extendedWaitUntil:
    visible:
      id: "home-unsubscribed-download-row-.*-delete-all"
    timeout: ${TIMEOUT_FASTEST}
- takeScreenshot: home-unsubscribed-swipe-delete-all
- tapOn:
    id: "home-unsubscribed-download-row-.*-delete-all"
- extendedWaitUntil:
    notVisible:
      id: home-unsubscribed-downloads
    timeout: ${TIMEOUT_FAST}
- takeScreenshot: home-unsubscribed-deleted
