appId: com.podverse.app.next
name: Album flow
---
# Album detail shows tracks. More → Go to track opens track detail (row press plays).

- runFlow: shared/launch-and-connect.yaml
- runFlow: shared/login-seeded-user.yaml

- openLink: podverse-next://album/e2eMusicAlbm01
- runFlow: shared/confirm-ios-open-dialog.yaml
- extendedWaitUntil:
    visible:
      id: album-detail-screen
    timeout: ${TIMEOUT_SLOWEST}
- extendedWaitUntil:
    visible:
      id: album-detail-track-list
    timeout: ${TIMEOUT_SLOW}
- extendedWaitUntil:
    visible:
      id: album-track-row-0
    timeout: ${TIMEOUT_SLOW}
- assertNotVisible:
    id: "home-row-play-.*"
- assertVisible:
    id: album-detail-section-tracks
- takeScreenshot: album-detail-tracks

- tapOn:
    id: "home-row-more-.*"
- extendedWaitUntil:
    visible:
      id: "media-row-menu-.*"
    timeout: ${TIMEOUT_FASTER}
- tapOn:
    id: "media-row-action-go-to-track-.*"
- extendedWaitUntil:
    visible:
      id: track-detail-screen
    timeout: ${TIMEOUT_SLOW}
- assertVisible:
    id: track-detail-album-header
- assertVisible:
    id: track-detail-playback-chrome
- assertVisible:
    id: track-detail-tab-summary
- takeScreenshot: album-to-track-detail
