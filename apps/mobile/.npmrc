# Standalone mobile install (outside the root npm workspace).
# Disables npm auto-install of peer deps so expo-dev-client peers (expo@*) do not pull
# latest expo@57 / react-native@0.86. Pinning is in apps/mobile/package.json overrides + deps.
legacy-peer-deps=true
