#####
##### podverse-web
#####
# Copy to .env.local. With the runtime-config sidecar, the app process needs RUNTIME_CONFIG_URL.
# make local_env_setup also copies Observability and Extensions keys from apps/web/sidecar/.env
# into .env.local for the Next.js server process.

#####
##### Runtime Config Sidecar
#####
RUNTIME_CONFIG_URL="http://localhost:3001"
