/**
 * Deterministic `account_set_password.set_password_token` seeded by `tools/web/seed-e2e.mjs`.
 * Keep the UUID string in sync with the seed script.
 */
export const E2E_SET_PASSWORD_INVITE_TOKEN = '11111111-1111-4111-8111-111111111111';
