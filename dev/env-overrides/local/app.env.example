# Log directory for API, Workers, and Management API. Empty = console-only.
LOG_DIR=

# Account signup mode: "contact-only" or "sign-up". When sign-up, mailer and token env are required.
ACCOUNT_SIGNUP_MODE="contact-only"
