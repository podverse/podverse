# Web app MetaBoost (MB1) runtime-config vars. Prepared by local_env_prepare; applied by local_env_setup.
# Optional: set both when testing MetaBoost flows locally (e.g. Donate page).

#####
##### App MetaBoost (Optional)
#####
NEXT_PUBLIC_APP_VALUE_METABOOST_STANDARD=
NEXT_PUBLIC_APP_VALUE_METABOOST_NODE=
